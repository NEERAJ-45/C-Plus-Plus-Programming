#ifndef VEHICLE_H
#define VEHICLE_H
using namespace std;
#include <string>

class Vehicle
{

private:
    int numWheels, maxGears, currentGears;
    bool carStatus;
    string color;
    float maxSpeed, currentSpeed;

public:
        Vehicle();
        Vehicle(int nu)
};
#endif